import SettingsClient from './SettingsClient'

export const metadata = {
  title: 'Settings - ShowTracker',
  description: 'Manage your app settings',
}

export default function SettingsPage() {
  return <SettingsClient />
}
